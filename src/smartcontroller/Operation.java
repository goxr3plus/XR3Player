package smartcontroller;

/**
 * Defines an operation.
 *
 * @author GOXR3PLUS
 */
public enum Operation {

    /** The move. */
    MOVE,
    /** The copy. */
    COPY,
    /** The close. */
    CLOSE,
    /** The open. */
    OPEN;
}